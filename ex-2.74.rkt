#lang racket

each division's file consists of a racket data
structure (each divion used a different data structure)

(headquarters (div-file (set-of-records (employee1 address
						   salary)
					(employee2 address
						   salary)
					(employee3 address
						   salary)
					...))
	      (div-file (set-of-records (employee1 address
						   salary)
					(employee2 address
						   salary)
					(employee3 address
						   salary)
					...))
	      (div-file (set-of-records (employee1 address
						   salary)
					(employee2 address
						   salary)
					(employee3 address
						   salary)
					...))
	      ...)

